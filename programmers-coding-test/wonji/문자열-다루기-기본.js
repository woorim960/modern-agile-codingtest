function solution(s) {
    
    const string = isNaN(s);
    if (s.length === 4 || s.length === 6){
        if (string === true){
            return false;
        } else if (string === false){
            return true;
        }
    } 
}
